export const SpeedDialActionSx = (theme: any) => ({
    marginTop: "16px",
    "& .MuiSpeedDialAction-fab": {
        backgroundColor: theme.palette.text.primary,
        color: theme.palette.background.default
    },
    "& .MuiSpeedDialAction-staticTooltipLabel": {
        backgroundColor: theme.palette.text.primary,
        color: theme.palette.background.default
    }
});
